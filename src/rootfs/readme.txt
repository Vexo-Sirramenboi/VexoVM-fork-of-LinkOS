Welcome to linkOS.

This is a portable programmer workstation that can turn itself into a data: URL.

Try: help, ls -l, cd /workspace, grep worker main.js, nano main.js, run main.js, ps, export.
Run `xdg-open /workspace/snake.html` to launch the Snake game in Browser.
